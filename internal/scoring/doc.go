// Verified Property Score (spec §13) — the ONLY package allowed to call the
// ml-sidecar FastAPI service or write property_scores. Logic arrives in a
// later phase; this is the skeleton package (LGF §2.1).
package scoring
