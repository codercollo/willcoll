// THE multi-tenancy package (spec §1a). Small and boring on purpose — this is
// the one package every other package's DB access implicitly trusts, so it does
// exactly one job and nothing else.
package tenancy
